% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitted_nethist.R
\name{fitted.multinethist}
\alias{fitted.multinethist}
\title{Fitted values for a multinethist model}
\usage{
\method{fitted}{multinethist}(
  object,
  set1 = NULL,
  set2 = NULL,
  layer = NULL,
  type = "nethist",
  drop = TRUE,
  ...
)
}
\arguments{
\item{object}{a \code{multinethist} object from \code{\link[=multinethist]{multinethist()}}.}

\item{set1}{integer vector of vertex indices for rows. \code{NULL} uses all vertices.}

\item{set2}{integer vector of vertex indices for columns. \code{NULL} uses all
vertices.}

\item{layer}{integer vector of layer indices. \code{NULL} uses all layers.}

\item{type}{one of \code{"nethist"} (default) or \code{"prob"}. \code{"nethist"} returns
\code{thetahat[,,l] / rho_hat[l]} per layer; \code{"prob"} returns raw \code{thetahat}.}

\item{drop}{logical. If \code{TRUE} (default) and a single layer is selected,
the layer dimension is dropped and a matrix is returned. If \code{FALSE}, a
3-dimensional array is always returned.}

\item{...}{currently unused.}
}
\value{
A numeric matrix of dimension \verb{|set1| x |set2|} when a single
layer is selected and \code{drop = TRUE}, otherwise a 3-dimensional array of
dimension \verb{|set1| x |set2| x |layer|}.
}
\description{
Returns a subarray of the fitted block-probability or graphon matrix
for a \code{multinethist} object.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
data(IndianVil)
fit <- multinethist(IndianVil, h = 20L)
fitted(fit, set1 = 1:10, set2 = 1:10, layer = 1)
fitted(fit, layer = c(1, 2), drop = FALSE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=multinethist]{multinethist()}}, \code{\link[=nethist]{nethist()}}
}
