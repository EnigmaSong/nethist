% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nethist_control.R
\name{nethist_control}
\alias{nethist_control}
\alias{print.nethist_control}
\title{Control parameters for network histogram algorithms}
\usage{
nethist_control(
  algorithm = "greedy",
  max_itr = 5e+06,
  greedy_swap_rule = "single_random",
  greedy_stop_threshold = 20000,
  verbose = FALSE,
  ...
)

\method{print}{nethist_control}(x, ...)
}
\arguments{
\item{algorithm}{character. Optimization algorithm. Currently only
\code{"greedy"} is implemented.}

\item{max_itr}{integer. Maximum number of iterations. Default is
\eqn{5 \times 10^6}.}

\item{greedy_swap_rule}{character. Node-pair selection rule for the greedy
search. At each iteration, two nodes are drawn and their block labels
are swapped if the move improves the objective. Currently only
\code{"single_random"} (one pair drawn uniformly at random) is
implemented.}

\item{greedy_stop_threshold}{integer. Early stopping criterion for the
greedy search; the algorithm terminates if the objective has not improved
for this many consecutive iterations. Default is 20,000.}

\item{verbose}{logical. Print progress messages during fitting.}

\item{...}{Accepts deprecated argument names \code{swap_rule} and
\code{consecutive_iter_threshold} with a warning.}

\item{x}{a \code{nethist_control} object.}
}
\value{
An object of class \code{"nethist_control"}.
}
\description{
Constructs a control object for \code{\link{nethist}},
\code{\link{multinethist}}, and \code{\link{hnethist}}.
}
\examples{
# default control object
ctrl <- nethist_control()
print(ctrl)

# reduce iteration limit for quick testing
ctrl <- nethist_control(max_itr = 1e4, greedy_stop_threshold = 100)

\donttest{
data(polblog)
fit <- nethist(polblog, control = nethist_control(max_itr = 1e4))
}
}
\seealso{
\code{\link{nethist}}, \code{\link{multinethist}},
\code{\link{hnethist}}
}
