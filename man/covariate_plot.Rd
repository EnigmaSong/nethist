% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariate_plot.R
\name{covariate_plot}
\alias{covariate_plot}
\alias{summary_plot}
\title{Bin summary by covariate}
\usage{
covariate_plot(
  object,
  covariate,
  idx_order = 1:max(object$cluster),
  main = NA,
  ylab = NA,
  legend_title = NA,
  stat = "count",
  position = "stack"
)

summary_plot(object, covariate, ...)
}
\arguments{
\item{object}{a \code{multinethist} object from \code{\link[=multinethist]{multinethist()}}.}

\item{covariate}{a vector for univariate covariate. If it is a factor, a stacked bar chart is drawn. If it is numeric, a violin plot is drawn.}

\item{idx_order}{A numeric vector for index label order, which must be a permutation of \code{object$cluster}. If \code{NA}, it uses \code{1:max(object$cluster)}.}

\item{main}{title of summary plot. If NA, the plot has no title.}

\item{ylab}{label of y-axis. If NA, y-axis label is "covariate"}

\item{legend_title}{title of legend. If NA, the legend title is "covariate"}

\item{stat}{variables pass to \code{\link[ggplot2:geom_bar]{ggplot2::geom_bar()}}. Only used for a factor covariate.}

\item{position}{variables pass to \code{\link[ggplot2:geom_bar]{ggplot2::geom_bar()}}. Only used for a factor covariate.}
}
\value{
A \code{ggplot} object. Printed as a side effect. Returns the plot invisibly.
}
\description{
Drawing a bin summary plot of covariates given a \code{multinethist} object with a user-specified order.
}
\details{
When \code{covariate} is a factor, a stacked bar chart is drawn with bins ordered by \code{idx_order}.
When \code{covariate} is numeric, a violin plot is drawn.
}
\examples{
{
set.seed(42)
data(polblog)
nethist_polblog <- multinethist(polblog)
x <- factor(c(rep("Liberal", 586), rep("Conservative", 638)))
covariate_plot(nethist_polblog, x)
}
}
