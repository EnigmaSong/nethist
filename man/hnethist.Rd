% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nethist_hybrid.R
\name{hnethist}
\alias{hnethist}
\title{Hybrid Network histogram estimation}
\usage{
hnethist(A, h = NA, method = "LSE", control = nethist_control(), ...)
}
\arguments{
\item{A}{An adjacency matrix or an igraph object. It must be an undirected and simple graph.}

\item{h}{A bandwidth parameter. If \code{NA}, the bandwidth is selected by Olhede and Wolfe (2014). If specified, the user-supplied value is used.}

\item{method}{Type of loss function for network histogram. Must be one of \code{LSE} (default) or \code{PLL} for single-layer hybrid network histogram. See Details.}

\item{control}{A control object from \code{\link{nethist_control}}. Governs
\code{max_itr}, \code{greedy_swap_rule}, \code{greedy_stop_threshold},
and \code{verbose}.}

\item{...}{Currently unused.}
}
\value{
A list of class \code{c("hnethist", "nethist")} with the following fields:

\itemize{
\item \code{cluster} an integer vector of length n with vertex-level block assignments from the initial nethist fit.
\item \code{thetahat} a k-by-k probability matrix of the selected hybrid model, where k is the number of nethist blocks.
\item \code{rho_hat} estimated sparsity parameter from the initial nethist fit.
\item \code{normalized_LL} normalized log-likelihood from the initial nethist fit.
\item \code{MSE} mean squared error of the selected model.
\item \code{method} loss function used (\code{"LSE"} or \code{"PLL"}).
\item \code{h} bandwidth used for the initial nethist fit.
\item \code{blockcluster} a \code{kmeans} object describing how the k-by-k blocks were merged into \code{s} shapes.
\item \code{BIC} BIC value of the selected model.
\item \code{s} number of distinct shapes in the selected model.
\item \code{details} list of all candidate models, one entry per number of shapes from 1 up to the maximum considered, each containing \code{s}, \code{blockcluster}, \code{thetahat}, \code{MSE}, \code{normalized_LL}, and \code{BIC}.
\item \code{initial} the nethist object used as the starting point for block clustering.
}
}
\description{
Estimating hybrid network histogram for single-layer networks and returning the indices of partitions.
}
\details{
{
Among the outputs, the best model is selected based on the BIC criterion.

The original reference provided theoretical guarantees for LSE, but we also allow PLL for the initial nethist fit. The block clustering and model selection steps are performed on the LSE loss regardless of the initial method, as the theoretical results pertain to LSE.
}
}
\references{
Verdeyme, A. & Olhede, S. C. (2024). Hybrid of Node and Link Communities for Graphon Estimation. arXiv:2401.05088
}
